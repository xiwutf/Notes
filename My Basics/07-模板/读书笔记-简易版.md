<%*
let bookTitle = tp.file.title;
if (/^(未命名|Untitled)/.test(bookTitle)) {
  const input = await tp.system.prompt("请输入书名（不用加 BOOK- 前缀）");
  if (input && input.trim()) {
    bookTitle = input.trim().startsWith("BOOK-") ? input.trim() : "BOOK-" + input.trim();
    await tp.file.rename(bookTitle);
  }
}
%>
---
type: reading
tags: [读书]
created: <% tp.date.now("YYYY-MM-DD") %>
updated: <% tp.date.now("YYYY-MM-DD") %>
status: 在读
author: 
domain: 
started: <% tp.date.now("YYYY-MM-DD") %>
finished: 
rating: 
---

# <% bookTitle %>

## 为什么读

> 读这本书的动机、期望解决什么问题



## 基本信息

| 项目 | 内容 |
|------|------|
| 作者 | |
| 类型 | 非虚构 / 虚构 / 技术 / 传记 / 其他 |
| 领域 | |
| 怎么知道这本书 | |
| 链接 | |

## 阅读进度

| 章节 | 状态 | 日期 | 一句话摘要 |
|------|------|------|------------|
| | 未读 / 在读 / 已读 | | |

**读到哪了**：

## 全书一句话



## 触动我的句子

| 原文 | 为什么打动我 |
|------|--------------|
| | |

## 核心收获

1. 
2. 
3. 

## 下一步行动

- [ ] 

## 我的评价

### 同意什么



### 质疑什么



---

**状态**：在读 / 已读 / 搁置
